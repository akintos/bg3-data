<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
			        xmlns:b="http://schemas.microsoft.com/xaml/behaviors"

					xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                    xmlns:System="clr-namespace:System;assembly=mscorlib"
                    xmlns:ls="clr-namespace:ls;assembly=SharedGUI"
                    xmlns:noesis="clr-namespace:NoesisGUIExtensions;assembly=Noesis.GUI.Extensions"
					mc:Ignorable="d">

    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="OptionTemplates_Shared.xaml"/>
        <ResourceDictionary Source="../Library/CharacterCreationLib_c.xaml"/>
    </ResourceDictionary.MergedDictionaries>

    <!--overwriting the base ScrollViewerTemplate cause that one uses a stackpanel resulting in the incorrect width but I can 't change that without loads of testing all other uis-->
    <ControlTemplate x:Key="ScrollViewerTemplate"  TargetType="ScrollViewer">
        <ControlTemplate.Resources>
            <Style TargetType="{x:Type ScrollBar}" BasedOn="{StaticResource DecorativeGoldScrollBar}"/>
            <GridLength x:Key="Top">0</GridLength>
            <GridLength x:Key="Bottom">0</GridLength>
            <GridLength x:Key="Left">0</GridLength>
            <GridLength x:Key="Right">0</GridLength>
        </ControlTemplate.Resources>

        <Grid Background="Transparent">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <ScrollContentPresenter x:Name="PART_ScrollContentPresenter" Grid.Column="0" Grid.Row="0" Content="{TemplateBinding Content}" ContentTemplate="{TemplateBinding ContentTemplate}" ContentTemplateSelector="{TemplateBinding ContentTemplateSelector}" 
                                    CanContentScroll="{TemplateBinding CanContentScroll}"/>

            <Grid Grid.Row="0" Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="{DynamicResource Top}"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="{DynamicResource Bottom}"/>
                </Grid.RowDefinitions>
                <ScrollBar x:Name="PART_VerticalScrollBar" Margin="{TemplateBinding Padding}" Grid.Row="1" Value="{TemplateBinding VerticalOffset}"  Maximum="{TemplateBinding ScrollableHeight}" ViewportSize="{TemplateBinding ViewportHeight}"  Visibility="{TemplateBinding ComputedVerticalScrollBarVisibility}"/>
            </Grid>

            <Grid Grid.Row="1" Grid.Column="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="{DynamicResource Left}"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="{DynamicResource Right}"/>
                </Grid.ColumnDefinitions>
                <ScrollBar x:Name="PART_HorizontalScrollBar" Orientation="Horizontal" Grid.Column="1" Value="{TemplateBinding HorizontalOffset}" Maximum="{TemplateBinding ScrollableWidth}" ViewportSize="{TemplateBinding ViewportWidth}"  Visibility="{TemplateBinding ComputedHorizontalScrollBarVisibility}"/>

            </Grid>
        </Grid>

    </ControlTemplate>


    <Style x:Key="SliderRepeatButtonStyle" TargetType="RepeatButton" BasedOn="{StaticResource {x:Type RepeatButton}}">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderBrush" Value="Transparent"/>
        <Setter Property="Focusable" Value="False"/>
    </Style>

    <Style x:Key="OptionSliderThumbStyle" TargetType="ls:LSThumb">
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="SoundID" Value="UI_HUD_Shared_Slider_Thumb"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ls:LSThumb">
                    <Grid>
                        <Image x:Name="img" Source="pack://application:,,,/GustavNoesisGUI;component/Assets/Buttons/btn_hudNav_d.png" Stretch="None"/>
                        <TextBlock VerticalAlignment="Center" HorizontalAlignment="Center" Margin="0,4,0,0"
                                   Text="{Binding Value, Converter={StaticResource RoundDownConverter}}" 
                                   Visibility="{Binding ShowValue, Converter={StaticResource BoolToVisibleConverter}}" 
                                   FontSize="{DynamicResource MediumFontSize}" Foreground="{DynamicResource LS_PrimaryColor}"
                                   TextAlignment="Center"/>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Source" Value="pack://application:,,,/GustavNoesisGUI;component/Assets/Buttons/btn_hudNav_h.png" TargetName="img"/>
                        </Trigger>
                        <Trigger Property="IsDragging" Value="True">
                            <Setter Property="Source" Value="pack://application:,,,/GustavNoesisGUI;component/Assets/Buttons/btn_hudNav_p.png" TargetName="img"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Source" Value="pack://application:,,,/GustavNoesisGUI;component/Assets/Buttons/btn_hudNav_disabled.png" TargetName="img"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>

        <Style.Triggers>
            <DataTrigger Binding="{Binding ShowValue}" Value="False">
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Thumb">
                            <Grid>
                                <Image x:Name="img" Source="pack://application:,,,/GustavNoesisGUI;component/Assets/Buttons/btn_hudNav_d.png" Stretch="None"/>
                                <Image x:Name="ico" Source="pack://application:,,,/GustavNoesisGUI;component/Assets/Options/ico_scrubber_d.png" Stretch="None"/>
                            </Grid>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Source" Value="pack://application:,,,/GustavNoesisGUI;component/Assets/Buttons/btn_hudNav_h.png" TargetName="img"/>
                                    <Setter Property="Source" Value="pack://application:,,,/GustavNoesisGUI;component/Assets/Options/ico_scrubber_h.png" TargetName="ico"/>
                                </Trigger>
                                <Trigger Property="IsDragging" Value="True">
                                    <Setter Property="Source" Value="pack://application:,,,/GustavNoesisGUI;component/Assets/Buttons/btn_hudNav_p.png" TargetName="img"/>
                                    <Setter Property="Source" Value="pack://application:,,,/GustavNoesisGUI;component/Assets/Options/ico_scrubber_h.png" TargetName="ico"/>
                                    <Setter Property="Margin" Value="0,2,0,-2" TargetName="ico"/>
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter Property="Source" Value="pack://application:,,,/GustavNoesisGUI;component/Assets/Buttons/btn_hudNav_disabled.png" TargetName="img"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </DataTrigger>
        </Style.Triggers>

    </Style>

    <Style x:Key="DefaultSliderValueStyle" TargetType="TextBlock">
        <Setter Property="Margin" Value="0,2,-38,-2" />
        <Setter Property="Text" Value="" />
        <Setter Property="Visibility" Value="Hidden" />
    </Style>

    <Style x:Key="PointNClickSliderValueStyle" BasedOn="{StaticResource PointNClickSliderValueSharedStyle}" TargetType="TextBlock">
        <Setter Property="Margin" Value="0,0,700,0"/>
    </Style>

    <Style x:Key="FontSizeSliderValueStyle" BasedOn="{StaticResource FontSizeSliderValueSharedStyle}" TargetType="TextBlock">
        <Setter Property="Margin" Value="0,0,900,0"/>
        <Setter Property="Tag" Value="{Binding Value, Converter={StaticResource RoundConverter}}"/>
    </Style>

    <ControlTemplate x:Key="OptionsSliderTemplate" TargetType="Slider">
        <ControlTemplate.Triggers>
            <DataTrigger Binding="{Binding Enabled}" Value="False">
                <Setter Property="Opacity" Value="{DynamicResource DisabledOpacity}" />
            </DataTrigger>
        </ControlTemplate.Triggers>
        <Grid HorizontalAlignment="Center" Width="660">
            <Image HorizontalAlignment="Center" VerticalAlignment="Center" 
                   Source="pack://application:,,,/GustavNoesisGUI;component/Assets/Options/input_slider_d.png" Stretch="None" />

            <ls:LSRepeatButton x:Name="PART_IncreaseRepeatButton" Style="{StaticResource SliderRepeatButtonStyle}" HorizontalAlignment="Right" Command="Slider.IncreaseLarge" BoundEvent="PanelSelect">
                <ls:LSRepeatButton.Content>
                    <Image Stretch="None" Source="pack://application:,,,/GustavNoesisGUI;component/Assets/Shared_c/c_carousel_arrow.png"/>
                </ls:LSRepeatButton.Content>
            </ls:LSRepeatButton>

            <ls:LSRepeatButton x:Name="PART_DecreaseRepeatButton" Style="{StaticResource SliderRepeatButtonStyle}" HorizontalAlignment="Left" Command="Slider.DecreaseLarge" BoundEvent="PartyManagement">
                <ls:LSRepeatButton.Content>
                    <Image Stretch="None" Source="pack://application:,,,/GustavNoesisGUI;component/Assets/Shared_c/c_carousel_arrow.png">
                        <Image.RenderTransform>
                            <ScaleTransform ScaleX="-1" CenterX="40"/>
                        </Image.RenderTransform>
                    </Image>
                </ls:LSRepeatButton.Content>
            </ls:LSRepeatButton>
            <ls:LSRepeatButton Visibility="Collapsed"
                               Command="Slider.DecreaseSmall" 
                               Focusable="False"
                               BoundEvent="UILeft"/>
            <ls:LSRepeatButton Visibility="Collapsed"
                               Command="Slider.IncreaseSmall"
                               Focusable="False"
                               BoundEvent="UIRight"/>


            <Track Name="PART_Track" 
                   HorizontalAlignment="Stretch" VerticalAlignment="Center" Margin="80,0"
                   Height="{TemplateBinding Height}">
                <Track.Thumb>
                    <ls:LSThumb Style="{StaticResource OptionSliderThumbStyle}" />
                </Track.Thumb>
            </Track>
        </Grid>
    </ControlTemplate>
    <ControlTemplate x:Key="OptionsSliderTemplateFocussable" TargetType="Slider">
        <Grid HorizontalAlignment="Center" Width="660">
            <Image HorizontalAlignment="Center" VerticalAlignment="Center" 
                   Source="pack://application:,,,/GustavNoesisGUI;component/Assets/Options/input_slider_d.png" Stretch="None" />

            <ls:LSRepeatButton x:Name="PART_IncreaseRepeatButton" Style="{StaticResource SliderRepeatButtonStyle}" HorizontalAlignment="Right" Command="Slider.IncreaseLarge" BoundEvent="PanelSelect">
                <ls:LSRepeatButton.Content>
                    <Image Stretch="None" Source="pack://application:,,,/GustavNoesisGUI;component/Assets/Shared_c/c_carousel_arrow.png"/>
                </ls:LSRepeatButton.Content>
            </ls:LSRepeatButton>

            <ls:LSRepeatButton x:Name="PART_DecreaseRepeatButton" Style="{StaticResource SliderRepeatButtonStyle}" HorizontalAlignment="Left" Command="Slider.DecreaseLarge" BoundEvent="PartyManagement">
                <ls:LSRepeatButton.Content>
                    <Image Stretch="None" Source="pack://application:,,,/GustavNoesisGUI;component/Assets/Shared_c/c_carousel_arrow.png">
                        <Image.RenderTransform>
                            <ScaleTransform ScaleX="-1" CenterX="40"/>
                        </Image.RenderTransform>
                    </Image>
                </ls:LSRepeatButton.Content>
            </ls:LSRepeatButton>
            <ls:LSRepeatButton x:Name="LeftBtn"
                               Visibility="Collapsed"
                               Command="Slider.DecreaseSmall" 
                               Focusable="False"
                               IsEnabled="False"
                               BoundEvent="UILeft"/>
            <ls:LSRepeatButton x:Name="RightBtn"
                               Visibility="Collapsed"
                               Command="Slider.IncreaseSmall"
                               IsEnabled="False"
                               Focusable="False"
                               BoundEvent="UIRight"/>


            <Track Name="PART_Track" 
                   HorizontalAlignment="Stretch" VerticalAlignment="Center" Margin="80,0"
                   Height="{TemplateBinding Height}">
                <Track.Thumb>
                    <ls:LSThumb Style="{StaticResource OptionSliderThumbStyle}" />
                </Track.Thumb>
            </Track>
        </Grid>
        <ControlTemplate.Triggers>
            <DataTrigger Binding="{Binding Enabled}" Value="False">
                <Setter Property="Opacity" Value="{DynamicResource DisabledOpacity}" />
            </DataTrigger>
            <DataTrigger Binding="{Binding IsFocused, RelativeSource={RelativeSource Self}}" Value="True">
                <Setter TargetName="LeftBtn" Property="IsEnabled" Value="True" />
                <Setter TargetName="RightBtn" Property="IsEnabled" Value="True" />
            </DataTrigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>


    <Style x:Key="OptionsComboBoxStyle" TargetType="{x:Type ls:LSComboBox}">
        <Setter Property="ItemContainerStyle" Value="{StaticResource ComboBoxItem}"/>
        <Setter Property="UseIndexSelection" Value="True"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ls:LSComboBox}">
                    <Grid Height="80">
                        <ToggleButton Focusable="False"
                                      Content="{Binding RelativeSource={RelativeSource AncestorType={x:Type ls:LSComboBox}}, Path=Text}" 
                                      IsChecked="{Binding Path=IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}">
                            <ToggleButton.Template>
                                <ControlTemplate TargetType="ToggleButton">
                                    <StackPanel Orientation="Horizontal">
                                        <Image x:Name="prevImg" HorizontalAlignment="Left" VerticalAlignment="Center" Stretch="None" 
                                               Source="pack://application:,,,/GustavNoesisGUI;component/Assets/Shared_c/c_carousel_arrow.png">
                                            <Image.RenderTransform>
                                                <ScaleTransform ScaleX="-1" CenterX="40"/>
                                            </Image.RenderTransform>
                                        </Image>
                                        <TextBlock VerticalAlignment="Center" HorizontalAlignment="Center" Width="500" TextAlignment="Center"
                                                   Text="{Binding Path=Content, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type ToggleButton}}}"  
                                                   Foreground="{DynamicResource LS_specialTxtColor}" FontSize="{DynamicResource DefaultFontSize}"
                                                   IsHitTestVisible="False"/>
                                        <Image x:Name="nextImg" HorizontalAlignment="Right" VerticalAlignment="Center" Stretch="None"
                                                Source="pack://application:,,,/GustavNoesisGUI;component/Assets/Shared_c/c_carousel_arrow.png"/>
                                    </StackPanel>
                                </ControlTemplate>
                            </ToggleButton.Template>
                        </ToggleButton>
                        <ListBox x:Name="RectList" ItemsSource="{Binding ItemsSource, RelativeSource={RelativeSource TemplatedParent}}" 
                                 SelectedIndex="{Binding SelectedIndex, RelativeSource={RelativeSource TemplatedParent}}" 
                                 VerticalAlignment="Bottom" HorizontalAlignment="Center" Focusable="False" MaxWidth="550">
                            <ListBox.Template>
                                <ControlTemplate>
                                    <ItemsPresenter/>
                                </ControlTemplate>
                            </ListBox.Template>
                            <ListBox.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel Orientation="Horizontal"/>
                                </ItemsPanelTemplate>
                            </ListBox.ItemsPanel>
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <Rectangle x:Name="rect" Width="30" Height="4" StrokeThickness="0" Fill="Gray"/>
                                    <DataTemplate.Triggers>
                                        <DataTrigger Binding="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type ListBoxItem}}, Path=IsSelected}" Value="True">
                                            <Setter Property="Fill" Value="{StaticResource LS_tint100}" TargetName="rect"/>
                                        </DataTrigger>
                                    </DataTemplate.Triggers>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                            <ListBox.ItemContainerStyle>
                                <Style TargetType="ListBoxItem" BasedOn="{StaticResource {x:Type ListBoxItem}}">
                                    <Setter Property="Focusable" Value="False"/>
                                    <Style.Triggers>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter Property="Background" Value="Transparent" />
                                            <Setter Property="BorderBrush" Value="Transparent" />
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </ListBox.ItemContainerStyle>
                        </ListBox>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <DataTrigger Binding="{Binding ItemsSource.Count, ElementName=RectList, Converter={StaticResource GreaterThanConverter}, ConverterParameter=10}" Value="True" >
                            <Setter TargetName="RectList" Property="Visibility" Value="Collapsed" />
                        </DataTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <DataTrigger Binding="{Binding Enabled}" Value="False">
                <Setter Property="Opacity" Value="{StaticResource DisabledOpacity}" />
            </DataTrigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="ButtonHintTextStyle" TargetType="TextBlock">
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="Foreground" Value="{DynamicResource LS_baseTxtColor}"/>
        <Setter Property="Margin" Value="0,0,-10,0"/>
    </Style>

    <ControlTemplate x:Key="ComboBoxTemplate">
        <ls:LSComboBox x:Name="comboBox" ItemsSource="{Binding ComboItems}" SelectedItem="{Binding SelectedItem}"
                       Style="{DynamicResource OptionsComboBoxStyle}" Text="{Binding SelectedItem.Name}" 
                       IsEnabled="{Binding Enabled}" Focusable="False" SoundID="UI_HUD_Options_ComboBox"
                       ActionNextEvent="UIRight" ActionPrevEvent="UILeft"/>
    </ControlTemplate>

    <ControlTemplate x:Key="TickBoxTemplate">
        <ls:LSTickBox Style="{DynamicResource TickBox}" IsChecked="{Binding Value}"
                           Command="{Binding DataContext.OnChangeToggleButton, RelativeSource={RelativeSource AncestorType={x:Type ls:UIWidget}}}"  
                           IsEnabled="{Binding Enabled}" Focusable="False" SoundID="UI_HUD_Options_ToggleButton"
                           ActionNextEvent="UIRight" ActionPrevEvent="UILeft"/>
    </ControlTemplate>

    <ControlTemplate x:Key="SliderTemplate">
        <Slider DataContext="{Binding}" 
                Template="{StaticResource OptionsSliderTemplate}"
                Value="{Binding Value}" 
                Minimum="{Binding Min}" 
                Maximum="{Binding Max}" 
                IsSnapToTickEnabled="False" 
                SmallChange="1" 
                LargeChange="{Binding Max, Converter={StaticResource DivideAndRoundConverter}, ConverterParameter='10'}" 
                IsEnabled="{Binding Enabled}"
                Focusable="False"/>
    </ControlTemplate>

    <ControlTemplate x:Key="EditBoxTemplate">
        <ls:LSTextBox Text="{Binding Value}"
                      IsEnabled="True"
                      MaxLength="{Binding MaxLength}"
                      TextImmediateSourceUpdate="True"
                      Foreground="{DynamicResource LS_specialTxtColor}"
                      FontSize="{DynamicResource DefaultFontSize}"
                      TextAlignment="Center"
                      Width="540"/>
    </ControlTemplate>

    <ControlTemplate x:Key="PasswordBoxTemplate">
        <ls:LSPasswordBox Text="{Binding Value}"
                          IsEnabled="True"
                          Focusable="False"
                          MaxLength="{Binding MaxLength}"
                          Foreground="{DynamicResource LS_specialTxtColor}"
                          FontSize="{DynamicResource DefaultFontSize}"
                          Width="540"/>
    </ControlTemplate>


    <ControlTemplate x:Key="ButtonTemplate">
        <ls:LSButton Style="{StaticResource ControllerButton}" Content="{Binding Path=DataContext, RelativeSource={RelativeSource Self}}" Focusable="False"
                         DataContext="{Binding DataContext.CurrentPlayer.UIData.InputEvents, Converter={StaticResource FindInputEventConverter}, ConverterParameter='UIMessageBoxX', RelativeSource={RelativeSource AncestorType={x:Type ls:UIWidget}}}"
                         Command="{Binding DataContext.ButtonCommand.ActionCommand, RelativeSource={RelativeSource AncestorType={x:Type ListBoxItem}}}"
                         IsEnabled="{Binding RelativeSource={RelativeSource AncestorType={x:Type ls:LSListBoxItem}}, Path=IsFocused, Converter={StaticResource BoolToVisibleConverter}}"/>
       
    </ControlTemplate>

    <DataTemplate x:Key="OptionTemplate">
        <ls:LSNineSliceImage x:Name="frame" MinHeight="112" Width="1650" Margin="0,2,0,2" ImageSource="pack://application:,,,/GustavNoesisGUI;component/Assets/Shared_c/c_listItem_wide_centered.png" Slices="4,4,4,4" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" >
            <Grid x:Name="Container" Margin="0,8"  >
                <Grid.ColumnDefinitions>
                    <ColumnDefinition x:Name="LabelColumn" Width="916"/>
                    <ColumnDefinition x:Name="ContentColumn" Width="*"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" VerticalAlignment="Center" HorizontalAlignment="Left"
                       Text="{Binding Text}" 
                       FontSize="{DynamicResource DefaultFontSize}" 
                       Foreground="{DynamicResource LS_accent75TxtColor}"
                       TextWrapping="Wrap"/>
                <TextBlock x:Name="PART_ValueLabel" Grid.Column="0" Grid.ColumnSpan="2" VerticalAlignment="Center" HorizontalAlignment="Right" TextAlignment="Right"
                       FontSize="{DynamicResource DefaultFontSize}"
                       Style="{StaticResource DefaultSliderValueStyle}" 
                       Foreground="{DynamicResource LS_accent100TxtColor}" />
                <Control Grid.Column="1" x:Name="content" IsEnabled="{Binding IsSelected, RelativeSource={RelativeSource AncestorType={x:Type ls:LSListBoxItem}}}"
                     VerticalAlignment="Center" HorizontalAlignment="Center" Focusable="False"
                     Template="{StaticResource ComboBoxTemplate}"/>

            </Grid>
        </ls:LSNineSliceImage>
        <DataTemplate.Triggers>
            <DataTrigger Binding="{Binding ListItemType}" Value="TickBox">
                <Setter Property="Template" Value="{StaticResource TickBoxTemplate}" TargetName="content"/>
            </DataTrigger>
            <DataTrigger Binding="{Binding ListItemType}" Value="Slider">
                <Setter Property="Template" Value="{StaticResource SliderTemplate}" TargetName="content"/>
            </DataTrigger>
            <MultiDataTrigger>
                <MultiDataTrigger.Conditions>
                    <Condition Binding="{Binding ListItemType}" Value="EditBox" />
                    <Condition Binding="{Binding HideValue}" Value="False"/>
                </MultiDataTrigger.Conditions>
                <Setter Property="Template" Value="{StaticResource EditBoxTemplate}" TargetName="content"/>
            </MultiDataTrigger>
            <MultiDataTrigger>
                <MultiDataTrigger.Conditions>
                    <Condition Binding="{Binding ListItemType}" Value="EditBox" />
                    <Condition Binding="{Binding HideValue}" Value="True"/>
                </MultiDataTrigger.Conditions>
                <Setter Property="Template" Value="{StaticResource PasswordBoxTemplate}" TargetName="content"/>
            </MultiDataTrigger>
            <DataTrigger Binding="{Binding ListItemType}" Value="Button">
                <Setter Property="Template" Value="{StaticResource ButtonTemplate}" TargetName="content"/>
                <Setter Property="Margin" Value="0,-8,0,-8" TargetName="content"/>
            </DataTrigger>
            <DataTrigger Binding="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type ls:LSListBoxItem}}, Path=IsSelected}" Value="True">
                <Setter Property="ImageSource" TargetName="frame" Value="pack://application:,,,/GustavNoesisGUI;component/Assets/Shared_c/c_listItemSelected_wide_centered.png"/>
            </DataTrigger>

            <DataTrigger Binding="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type ls:UIWidget}}, Path=ContextName}" Value="VideoOptions">
                <Setter Property="Width" TargetName="LabelColumn" Value="500"/>
            </DataTrigger>
            <DataTrigger Binding="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type ls:UIWidget}}, Path=ContextName}" Value="InterfaceOptions">
                <Setter Property="Width" TargetName="LabelColumn" Value="500"/>
            </DataTrigger>
            <DataTrigger Binding="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type ls:UIWidget}}, Path=ContextName}" Value="LobbyBrowser">
                <Setter Property="Width" TargetName="LabelColumn" Value="450"/>
                <Setter Property="Width" TargetName="frame" Value="1200"/>
            </DataTrigger>
            <DataTrigger Binding="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type ls:UIWidget}}, Path=ContextName}" Value="Lobby">
                <Setter Property="Width" TargetName="LabelColumn" Value="450"/>
                <Setter Property="Width" TargetName="frame" Value="1200"/>
            </DataTrigger>
            <DataTrigger Binding="{Binding OptionType}" Value="POINT_AND_CLICK_FREQUENCY">
                <Setter Property="Style" Value="{StaticResource PointNClickSliderValueStyle}" TargetName="PART_ValueLabel"/>
            </DataTrigger>
            <DataTrigger Binding="{Binding OptionType}" Value="DIALOG_FONT_SIZE">
                <Setter Property="Style" Value="{StaticResource FontSizeSliderValueStyle}" TargetName="PART_ValueLabel"/>
            </DataTrigger>
            <DataTrigger Binding="{Binding OptionType}" Value="OVERHEAD_FONT_SIZE">
                <Setter Property="Style" Value="{StaticResource FontSizeSliderValueStyle}" TargetName="PART_ValueLabel"/>
            </DataTrigger>
        </DataTemplate.Triggers>
    </DataTemplate>

    <ControlTemplate x:Key="TextButtonTemplate" TargetType="ls:LSButton">
        <Grid>
            <Image x:Name="img" Source="{DynamicResource ButtonNormal}" Stretch="None"/>
            <TextBlock x:Name="txt" Margin="0,0,0,0" VerticalAlignment="Center" HorizontalAlignment="Center" Text="{Binding Content, RelativeSource={RelativeSource Mode=TemplatedParent}}" Foreground="{TemplateBinding Foreground}" FontSize="{TemplateBinding FontSize}"/>
        </Grid>
        <ControlTemplate.Triggers>
            <Trigger Property="IsPressed" Value="True">
                <Setter Property="Source" Value="{DynamicResource ButtonPressed}" TargetName="img"/>
                <Setter Property="Margin" Value="0,8,0,0" TargetName="txt"/>
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Opacity" Value="0.7" TargetName="txt"/>
                <Setter Property="Source" Value="{DynamicResource ButtonDisabled}" TargetName="img"/>
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <DataTemplate x:Key="KeybindingTemplate">
        <Grid Margin="0,4,0,4">
            <Grid.Resources>
                <Style TargetType="{x:Type ls:LSButton}">
                    <Style.Resources>
                        <ImageSource x:Key="ButtonNormal">pack://application:,,,/GustavNoesisGUI;component/Assets/Options/inputBox_keybinds_1.png</ImageSource>
                        <ImageSource x:Key="ButtonHighlight">pack://application:,,,/GustavNoesisGUI;component/Assets/Options/inputBox_keybinds_1_active.png</ImageSource>
                        <ImageSource x:Key="ButtonPressed">pack://application:,,,/GustavNoesisGUI;component/Assets/Options/inputBox_keybinds_1_active.png</ImageSource>
                        <ImageSource x:Key="ButtonDisabled">pack://application:,,,/GustavNoesisGUI;component/Assets/Options/inputBox_keybinds_1_active.png</ImageSource>
                    </Style.Resources>
                    <Setter Property="Template" Value="{DynamicResource TextButtonTemplate}"/>
                    <Setter Property="FontSize" Value="{StaticResource BigFontSize}"/>
                    <Setter Property="Foreground" Value="{DynamicResource LS_PrimaryColor}"/>
                </Style>
            </Grid.Resources>
            <Image Stretch="None" Source="pack://application:,,,/GustavNoesisGUI;component/Assets/Options/entryFrame_wide.png"/>
            <Image x:Name="highlight" Stretch="None" Source="pack://application:,,,/GustavNoesisGUI;component/Assets/Options/selector_wide.png" Visibility="Hidden"/>
            <TextBlock VerticalAlignment="Center" HorizontalAlignment="Left" Margin="160,0,0,0" Text="{Binding Text}"
                       FontSize="{DynamicResource DefaultFontSize}" Foreground="{DynamicResource LS_PrimaryColor}"/>
            <ItemsControl VerticalAlignment="Center" HorizontalAlignment="Right" Margin="0,0,160,0" ItemsSource="{Binding Bindings}" 
                      Background="Transparent" BorderBrush="Transparent" 
                      ScrollViewer.HorizontalScrollBarVisibility="Disabled" >

                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <VirtualizingStackPanel IsItemsHost="True" Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <ls:LSButton Content="{Binding BindingString}" 
                                     Command="{Binding DataContext.RequestKeybindingChange, RelativeSource={RelativeSource AncestorType={x:Type ls:UIWidget}}}" 
                                     CommandParameter="{Binding}" SoundID="UI_HUD_Options_RebindElement"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>

            </ItemsControl>
        </Grid>
        <DataTemplate.Triggers>
            <DataTrigger Binding="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type ListBoxItem}}, Path=IsSelected}" Value="True">
                <Setter Property="Visibility" TargetName="highlight" Value="Visible"/>
            </DataTrigger>
        </DataTemplate.Triggers>
    </DataTemplate>

    <DataTemplate x:Key="OptionButton">
        <StackPanel Orientation="Horizontal">
            <ls:LSButton Style="{StaticResource ControllerButton}" Content="" Focusable="False"
                         DataContext="{Binding DataContext.CurrentPlayer.UIData.InputEvents, Converter={StaticResource FindInputEventConverter}, ConverterParameter='UIMessageBoxX', RelativeSource={RelativeSource AncestorType={x:Type ls:UIWidget}}}"
                         Command="{Binding DataContext.ButtonCommand.ActionCommand, RelativeSource={RelativeSource AncestorType={x:Type ListBoxItem}}}"
                         Visibility="{Binding RelativeSource={RelativeSource AncestorType={x:Type ls:LSListBoxItem}}, Path=IsFocused, Converter={StaticResource BoolToVisibleConverter}, ConverterParameter=True}"
                         IsEnabled="{Binding RelativeSource={RelativeSource AncestorType={x:Type ls:LSListBoxItem}}, Path=IsFocused, Converter={StaticResource BoolToVisibleConverter}, ConverterParameter=True}"/>
            <TextBlock Text="{Binding Text}" Margin="0,8" VerticalAlignment="Center" FontSize="{DynamicResource MediumFontSize}" Foreground="{DynamicResource LS_tint100}"/>
        </StackPanel>
    </DataTemplate>

    <DataTemplate x:Key="OptionTitle">
        <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center" TextAlignment="Center"
                   Margin="0,40,0,0"
                   Text="{Binding Text}" 
                   FontSize="{DynamicResource DefaultFontSize}"
                   Foreground="{DynamicResource LS_tint100}"/>
    </DataTemplate>

    <!-- Options -->
    <Style x:Key="OptionStyle" TargetType="ListBoxItem"  BasedOn="{StaticResource {x:Type ListBoxItem}}">
        <Setter Property="ContentTemplate" Value="{StaticResource OptionTemplate}" />
        <Setter Property="HorizontalAlignment" Value="Center"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="BorderBrush" Value="Transparent"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Style.Triggers>
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="BorderBrush" Value="Transparent" />
                <Setter Property="Background" Value="Transparent" />
            </Trigger>
            <DataTrigger Binding="{Binding ListItemType}" Value="Keybinding">
                <Setter Property="ContentTemplate" Value="{StaticResource KeybindingTemplate}"/>
            </DataTrigger>
            <DataTrigger Binding="{Binding ListItemType}" Value="String">
                <Setter Property="ContentTemplate" Value="{StaticResource OptionTitle}"/>
                <!-- The titles are not selectable, so they are disabled to skip the selection.-->
                <Setter Property="IsEnabled" Value="False"/>
            </DataTrigger>

            <!-- Controller doesn't have tooltip pinning yet so we hide the option and force the description to always show in the Tooltips.xaml -->
            <DataTrigger Binding="{Binding OptionType}" Value="EXTRA_TOOLTIP_DESCRIPTIONS">
                <Setter Property="Visibility" Value="Collapsed"/>
            </DataTrigger>

            <!-- Disabled show pings option, until front-end part is done -->
            <DataTrigger Binding="{Binding OptionType}" Value="SHOW_PINGS">
                <Setter Property="Visibility" Value="Collapsed"/>
            </DataTrigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="PreviewTextStyle" TargetType="TextBlock">
        <Setter Property="TextAlignment" Value="Center" />
        <Setter Property="FontSize" Value="{DynamicResource MediumFontSize}" />
        <Setter Property="Foreground" Value="{StaticResource LS_specialTxtColor}" />
    </Style>

    <Style x:Key="OptionPreviewGridMarginStyle" TargetType="Grid">
        <Setter Property="Margin" Value="0,8,0,0"/>
    </Style>

    <ControlTemplate x:Key="OptionTopButtons">
        <StackPanel Orientation="Horizontal" VerticalAlignment="Top" HorizontalAlignment="Center" Height="190">
            <ls:LSButton Style="{DynamicResource ControllerButton}" HorizontalAlignment="Right" Margin="0,0,54,0" VerticalAlignment="Center"
                         DataContext="{Binding CurrentPlayer.UIData.InputEvents, Converter={StaticResource FindInputEventConverter}, ConverterParameter='UITabPrev'}"
                         Content="" EatInput="False"/>
            
            <ls:LSListBox x:Name="ButtonsList"
                          IsEnabled="{Binding IsEnabled, RelativeSource={RelativeSource Mode=TemplatedParent}}"
                          VerticalAlignment="Center" HorizontalAlignment="Center" Focusable="False"
                          ActionPrevEvent="UITabPrev" ActionNextEvent="UITabNext">
                <ls:LSListBox.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ls:LSListBox.ItemsPanel>
                <ls:LSListBox.ItemContainerStyle>
                    <Style TargetType="ls:LSListBoxItem" BasedOn="{StaticResource {x:Type ls:LSListBoxItem}}">
                        <Setter Property="Background" Value="Transparent" />
                        <Setter Property="BorderBrush" Value="Transparent" />
                        <Setter Property="ContentTemplate">
                            <Setter.Value>
                                <DataTemplate>
                                    <TextBlock x:Name="label" Text="{Binding DataContext.Label ,Converter={StaticResource TranslatedStringConverter}, RelativeSource={RelativeSource AncestorType={x:Type ls:LSListBoxItem}}}" 
                                               Foreground="{DynamicResource LS_accent75TxtColor}" FontSize="{StaticResource TitleFontSize}" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="56,0"/>
                                    <DataTemplate.Triggers>
                                        <DataTrigger Binding="{Binding RelativeSource={RelativeSource Mode=TemplatedParent}, Path=IsSelected}" Value="True">
                                            <Setter Property="Foreground" Value="{DynamicResource LS_tint100}" TargetName="label"/>
                                        </DataTrigger>
                                    </DataTemplate.Triggers>
                                </DataTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="Transparent" />
                                <Setter Property="BorderBrush" Value="Transparent" />
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </ls:LSListBox.ItemContainerStyle>
                <b:Interaction.Triggers>
                    <b:EventTrigger EventName="SelectionChanged">
                        <b:InvokeCommandAction IsEnabled="{Binding HasChanges, Converter={StaticResource InvertBoolConverter}}"
                                               Command="{Binding SetActiveTab}" 
                                               CommandParameter="{Binding ElementName=ButtonsList, Path=SelectedItem.DataContext}"/>

                        <b:InvokeCommandAction IsEnabled="{Binding HasChanges}"
                                               Command="{Binding ShowCloseWithoutSaveCommand}" 
                                               CommandParameter="{Binding ElementName=ButtonsList, Path=SelectedItem.DataContext}"/>
                    </b:EventTrigger>
                </b:Interaction.Triggers>

                <ls:LSListBoxItem DataContext="{Binding Source={StaticResource gameplayOptionInfo}}"/>
                <ls:LSListBoxItem DataContext="{Binding Source={StaticResource keybindingOptionInfo}}" Visibility="Collapsed"/>
                <ls:LSListBoxItem DataContext="{Binding Source={StaticResource controllerOptionInfo}}"/>
                <ls:LSListBoxItem DataContext="{Binding Source={StaticResource videoOptionInfo}}"/>
                <ls:LSListBoxItem DataContext="{Binding Source={StaticResource audioOptionInfo}}"/>
                <ls:LSListBoxItem DataContext="{Binding Source={StaticResource interfaceOptionInfo}}"/>
            </ls:LSListBox>

            <ls:LSButton Style="{DynamicResource ControllerButton}" HorizontalAlignment="Left" Margin="10,0,0,0" VerticalAlignment="Center"
                         DataContext="{Binding CurrentPlayer.UIData.InputEvents, Converter={StaticResource FindInputEventConverter}, ConverterParameter='UITabNext'}"
                         Content="" EatInput="False"/>
        </StackPanel>
        <ControlTemplate.Triggers>
            <DataTrigger Binding="{Binding ActiveOptionsID}" Value="GameOptions">
                <Setter TargetName="ButtonsList" Property="SelectedIndex" Value="0"/>
            </DataTrigger>
            <DataTrigger Binding="{Binding ActiveOptionsID}" Value="KeybindingOptions">
                <Setter TargetName="ButtonsList" Property="SelectedIndex" Value="1"/>
            </DataTrigger>
            <DataTrigger Binding="{Binding ActiveOptionsID}" Value="ControllerOptions">
                <Setter TargetName="ButtonsList" Property="SelectedIndex" Value="2"/>
            </DataTrigger>
            <DataTrigger Binding="{Binding ActiveOptionsID}" Value="VideoOptions">
                <Setter TargetName="ButtonsList" Property="SelectedIndex" Value="3"/>
            </DataTrigger>
            <DataTrigger Binding="{Binding ActiveOptionsID}" Value="AudioOptions">
                <Setter TargetName="ButtonsList" Property="SelectedIndex" Value="4"/>
            </DataTrigger>
            <DataTrigger Binding="{Binding ActiveOptionsID}" Value="InterfaceOptions">
                <Setter TargetName="ButtonsList" Property="SelectedIndex" Value="5"/>
            </DataTrigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <Style x:Key="ButtonPromptDisabledStyle" TargetType="TextBlock">
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="Opacity" Value="1"/>
        <Style.Triggers>
            <DataTrigger Binding="{Binding IsEnabled, ElementName=HoldApplySlotButton}" Value="False">
                <Setter Property="Opacity" Value="{StaticResource ControllerButton.DisabledOpacity}"/>
            </DataTrigger>
        </Style.Triggers>
    </Style>

    <ControlTemplate x:Key="OptionBottomButtons">
        <StackPanel Orientation="Horizontal" Height="180">
            <StackPanel x:Name="ApplyButtonHolder" Orientation="Horizontal" VerticalAlignment="Center" Margin="16,0">
                <TextBlock Text="{Binding Source='hd9ef0e80g254cg4b35gb1e7gadfd05fe9f25', Converter={StaticResource TranslatedStringConverter}}" 
                           Foreground="{DynamicResource LS_baseTxtColor}" Style="{StaticResource ButtonPromptDisabledStyle}"/>

                <TextBlock Text="{Binding Source='hba9c74bbg1f89g4f68gbafbg1bcf7442cfae',Converter={StaticResource TranslatedStringConverter}, StringFormat='({0})'}" 
                           Foreground="{DynamicResource LS_InputTypeColor}" Margin="10,0,-20,0" Style="{StaticResource ButtonPromptDisabledStyle}"/>

                <Grid HorizontalAlignment="Right" x:Name="HoldApplySlotButtonHolder">
                    <Grid.Resources>
                        <Storyboard x:Key="HoldApplySlotAnim">
                            <DoubleAnimationUsingKeyFrames Storyboard.TargetProperty="(ls:LSPie.Value)" Storyboard.TargetName="HoldApplySlotPie" BeginTime="0:0:0.3">
                                <EasingDoubleKeyFrame KeyTime="0" Value="0"/>
                                <EasingDoubleKeyFrame KeyTime="0:0:1" Value="1"/>
                            </DoubleAnimationUsingKeyFrames>
                        </Storyboard>
                    </Grid.Resources>

                    <ls:LSButton x:Name="HoldApplySlotButton" EatInput="False" TapTime="300"
                                 Style="{DynamicResource ControllerButton}"
                                 DataContext="{Binding CurrentPlayer.UIData.InputEvents, Converter={StaticResource FindInputEventConverter}, ConverterParameter='UIAccept'}"
                                 Content=""
                                 SoundID="UI_HUD_Options_Accept"
                                 IsEnabled="{Binding DataContext.HasChanges, RelativeSource={RelativeSource AncestorType={x:Type ls:UIWidget}}}">

                        <b:Interaction.Triggers>
                            <b:EventTrigger EventName="LSButtonPressed">
                                <b:ControlStoryboardAction Storyboard="{StaticResource HoldApplySlotAnim}"/>
                            </b:EventTrigger>
                            <b:EventTrigger EventName="LSButtonReleased">
                                <b:ControlStoryboardAction Storyboard="{StaticResource HoldApplySlotAnim}" ControlStoryboardOption="Stop"/>
                            </b:EventTrigger>
                            <b:StoryboardCompletedTrigger Storyboard="{StaticResource HoldApplySlotAnim}">
                                <b:InvokeCommandAction Command="{Binding DataContext.ApplyCommand, RelativeSource={RelativeSource AncestorType={x:Type ls:UIWidget}}}"/>
                                <b:ControlStoryboardAction Storyboard="{StaticResource HoldApplySlotAnim}" ControlStoryboardOption="Stop"/>
                            </b:StoryboardCompletedTrigger>
                        </b:Interaction.Triggers>
                    </ls:LSButton>

                    <ls:LSPie x:Name="HoldApplySlotPie" Value="0" SweepDirection="Clockwise" HorizontalAlignment="Right" Margin="0,0,26,0"
                              Visibility="{Binding ElementName=HoldApplySlotButton, Path=IsPressed, Converter={StaticResource BoolToVisibleConverter}}">
                        <Image Source="pack://application:,,,/GustavNoesisGUI;component/Assets/Shared_c/c_tabSelector.png" Width="100" Height="100"/>
                    </ls:LSPie>
                </Grid>
            </StackPanel>

            <StackPanel x:Name="DefaultButtonHolder" Orientation="Horizontal" VerticalAlignment="Center" Margin="16,0">
                <TextBlock Text="{Binding Source='h0dcd297egde63g4e44g820dg07c21a2346d3',Converter={StaticResource TranslatedStringConverter}}" 
                           Foreground="{DynamicResource LS_baseTxtColor}" VerticalAlignment="Center"/>
                
                <TextBlock Text="{Binding Source='hba9c74bbg1f89g4f68gbafbg1bcf7442cfae',Converter={StaticResource TranslatedStringConverter}, StringFormat='({0})'}" 
                           Foreground="{DynamicResource LS_InputTypeColor}" VerticalAlignment="Center" Margin="10,0,-20,0"/>

                <Grid HorizontalAlignment="Right" x:Name="HoldDefaultButtonHolder">
                    <Grid.Resources>
                        <Storyboard x:Key="HoldDefaultAnim">
                            <DoubleAnimationUsingKeyFrames Storyboard.TargetProperty="(ls:LSPie.Value)" Storyboard.TargetName="HoldDefaultPie" BeginTime="0:0:0.3">
                                <EasingDoubleKeyFrame KeyTime="0" Value="0"/>
                                <EasingDoubleKeyFrame KeyTime="0:0:1" Value="1"/>
                            </DoubleAnimationUsingKeyFrames>
                        </Storyboard>
                    </Grid.Resources>

                    <ls:LSButton x:Name="HoldDefaultButton" EatInput="False" TapTime="300" Style="{DynamicResource ControllerButton}"   
                                 DataContext="{Binding CurrentPlayer.UIData.InputEvents, Converter={StaticResource FindInputEventConverter}, ConverterParameter='UICreate'}"
                                 Content=""
                                 SoundID="UI_HUD_Options_Default">
                        <b:Interaction.Triggers>
                            <b:EventTrigger EventName="LSButtonPressed">
                                <b:ControlStoryboardAction Storyboard="{StaticResource HoldDefaultAnim}"/>
                            </b:EventTrigger>
                            <b:EventTrigger EventName="LSButtonReleased">
                                <b:ControlStoryboardAction Storyboard="{StaticResource HoldDefaultAnim}" ControlStoryboardOption="Stop"/>
                            </b:EventTrigger>
                            <b:StoryboardCompletedTrigger Storyboard="{StaticResource HoldDefaultAnim}">
                                <b:InvokeCommandAction Command="{Binding DataContext.ResetOptionsToDefaultCommand, RelativeSource={RelativeSource AncestorType={x:Type ls:UIWidget}}}"/>
                                <b:ControlStoryboardAction Storyboard="{StaticResource HoldDefaultAnim}" ControlStoryboardOption="Stop"/>
                            </b:StoryboardCompletedTrigger>
                        </b:Interaction.Triggers>
                    </ls:LSButton>

                    <ls:LSPie x:Name="HoldDefaultPie" Value="0" SweepDirection="Clockwise" HorizontalAlignment="Right" Margin="0,0,26,0"
                              Visibility="{Binding ElementName=HoldDefaultButton, Path=IsPressed, Converter={StaticResource BoolToVisibleConverter}}">
                        <Image Source="pack://application:,,,/GustavNoesisGUI;component/Assets/Shared_c/c_tabSelector.png" Width="100" Height="100"/>
                    </ls:LSPie>
                </Grid>
            </StackPanel>

            <StackPanel Orientation="Horizontal" Margin="16,0">
                <TextBlock Text="{Binding Source='ha6d53a36ga066g4e6ag9c62g4335bd3dea59',Converter={StaticResource TranslatedStringConverter}}" Style="{StaticResource ButtonHintTextStyle}"/>
                <ls:LSButton Style="{StaticResource ControllerButton}" VerticalAlignment="Center" EatInput="False"
                             DataContext = "{Binding CurrentPlayer.UIData.InputEvents, Converter={StaticResource FindInputEventConverter}, ConverterParameter='UILeft'}"
                             Content="" />
            </StackPanel>


            <StackPanel Orientation="Horizontal" Margin="16,0">
                <TextBlock Text="{Binding Source='h5c405124g4622g4197g9239gb635f371bf0e',Converter={StaticResource TranslatedStringConverter}}" Style="{StaticResource ButtonHintTextStyle}"/>
                <ls:LSButton Style="{StaticResource ControllerButton}" VerticalAlignment="Center" EatInput="False"
                             DataContext = "{Binding CurrentPlayer.UIData.InputEvents, Converter={StaticResource FindInputEventConverter}, ConverterParameter='UIUp'}"
                             Content="" />
            </StackPanel>

            <StackPanel Orientation="Horizontal" Margin="16,0">
                <TextBlock Text="{Binding Source='hce6efd68gaa45g4da2gaf87g58a6d03bb3f7',Converter={StaticResource TranslatedStringConverter}}" Style="{StaticResource ButtonHintTextStyle}"/>
                <ls:LSButton Style="{DynamicResource ControllerButton}" 
                             DataContext="{Binding CurrentPlayer.UIData.InputEvents, Converter={StaticResource FindInputEventConverter}, ConverterParameter='UICancel'}"
                             Content=""
                             SoundID="UI_HUD_Options_Close"
                             Command="{Binding DataContext.CancelCommand, RelativeSource={RelativeSource AncestorType={x:Type ls:UIWidget}}}"/>
            </StackPanel>
        </StackPanel>
    </ControlTemplate>

    <ControlTemplate x:Key="SettingsChangeNotificationTemplate">
        <Grid>
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Bottom" Visibility="{Binding HasChanges, Converter={StaticResource BoolToVisibleConverter}, ConverterParameter='True'}" Margin="0,0,0,60">
                <Image Source="pack://application:,,,/GustavNoesisGUI;component/Assets/Profiles/profiles_divider.png" Stretch="None" Margin="8,0,-8,0"/>
                <TextBlock Foreground="{StaticResource LS_accent75TxtColor}" Text="{Binding Source='h9780697dg52a7g41efg8505g63172f1c03ad', Converter={StaticResource TranslatedStringConverter}}" TextAlignment="Center"/>
            </StackPanel>
            <Image Source="pack://application:,,,/GustavNoesisGUI;component/Assets/Shared_c/c_panel_bottom_standard.png" Stretch="None" VerticalAlignment="Bottom" HorizontalAlignment="Center"/>
        </Grid>
    </ControlTemplate>

    <ControlTemplate x:Key="StandardOptionsTemplate">
        <Grid HorizontalAlignment="Center" VerticalAlignment="Center" Height="{StaticResource DesignedWindowHeight}" Width="{StaticResource DesignedWindowWidth}">
            <Image Source="pack://application:,,,/GustavNoesisGUI;component/Assets/Shared/dialog_top.png" Stretch="None" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,-120,0,0"/>
            <Control DataContext="{Binding DataContext, RelativeSource={RelativeSource AncestorType={x:Type ls:UIWidget}}}" Template="{StaticResource OptionTopButtons}"/>

            <Grid HorizontalAlignment="Center" VerticalAlignment="Center" Margin="80,0,0,40">
                <ls:LSListBox x:Name="OptionsList"
                              AlternationCount="{Binding Options.Count}" 
                              SelectedIndex="1"
                              ItemsSource="{Binding Options}" 
                              ItemContainerStyle="{StaticResource OptionStyle}" 
                              Background="Transparent" BorderBrush="Transparent" 
                              Height="1600"
                              ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                              LocalFocusSelector="{Binding ElementName=Selector,Mode=OneWay}"
                              KeyboardNavigation.DirectionalNavigation="Continue"
                              AutoScrollSelectedIntoView="True"
                              ActionPrevEvent="UIUp" ActionNextEvent="UIDown"
                              VirtualizingPanel.IsVirtualizing="False">
                    <ls:LSListBox.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel />
                        </ItemsPanelTemplate>
                    </ls:LSListBox.ItemsPanel>
                    <ls:LSListBox.Template>
                        <ControlTemplate>
                            <ScrollViewer Template="{StaticResource ScrollViewerTemplate}" Padding="4,0,0,0" HorizontalAlignment="Stretch" Focusable="False">
                                <ScrollViewer.Resources>
                                    <GridLength x:Key="Top">84</GridLength>
                                    <GridLength x:Key="Bottom">10</GridLength>
                                </ScrollViewer.Resources>
                                <ItemsPresenter />
                            </ScrollViewer>
                        </ControlTemplate>
                    </ls:LSListBox.Template>
                </ls:LSListBox>

                <Control x:Name="Selector" IsHitTestVisible="False" VerticalAlignment="Center" HorizontalAlignment="Center" Template="{StaticResource SelectorTemplate}" Visibility="Hidden"/>
            </Grid>

            <Control Template="{StaticResource SettingsChangeNotificationTemplate}" VerticalAlignment="Bottom" Margin="0,0,0,140"/>

            <Control Template="{StaticResource OptionBottomButtons}" VerticalAlignment="Bottom" HorizontalAlignment="Center"/>
        </Grid>
    </ControlTemplate>

    <ControlTemplate x:Key="PreviewOptionsTemplate">
        <Grid HorizontalAlignment="Center"  Height="{StaticResource DesignedWindowHeight}" Width="{StaticResource DesignedWindowWidth}">
            <Control DataContext="{Binding DataContext, RelativeSource={RelativeSource AncestorType={x:Type ls:UIWidget}}}" Template="{StaticResource OptionTopButtons}"/>
            <Image Source="pack://application:,,,/GustavNoesisGUI;component/Assets/Shared/dialog_top.png" Stretch="None" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,-120,0,0"/>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                <Grid VerticalAlignment="Center" Margin="0,0,-200,0">
                    <ls:LSListBox x:Name="Options"
                              AlternationCount="{Binding Options.Count}" 
                              SelectedIndex="1"
                              ItemsSource="{Binding Options}" 
                              ItemContainerStyle="{StaticResource OptionStyle}" 
                              Background="Transparent" BorderBrush="Transparent" 
                              Height="1600"
                              HorizontalAlignment="Center" VerticalAlignment="Center"
                              ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                              LocalFocusSelector="{Binding ElementName=Selector,Mode=OneWay}"
                              KeyboardNavigation.DirectionalNavigation="Continue"
                              AutoScrollSelectedIntoView="True"
                              ActionPrevEvent="UIUp" ActionNextEvent="UIDown"
                              VirtualizingPanel.IsVirtualizing="False">
                        <ls:LSListBox.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel />
                            </ItemsPanelTemplate>
                        </ls:LSListBox.ItemsPanel>
                        <ls:LSListBox.Template>
                            <ControlTemplate>
                                <ScrollViewer Template="{StaticResource FlippedScrollViewerTemplate}" Padding="38,0,0,0" Focusable="False">
                                    <ScrollViewer.Resources>
                                        <GridLength x:Key="Top">84</GridLength>
                                        <GridLength x:Key="Bottom">84</GridLength>
                                    </ScrollViewer.Resources>
                                    <ItemsPresenter/>
                                </ScrollViewer>
                            </ControlTemplate>
                        </ls:LSListBox.Template>
                    </ls:LSListBox>

                    <Control x:Name="Selector" IsHitTestVisible="False" VerticalAlignment="Center" HorizontalAlignment="Center" Template="{StaticResource SelectorTemplate}" Visibility="Hidden"/>
                </Grid>

                <Image Source="pack://application:,,,/GustavNoesisGUI;component/Assets/Options/options_bgLine.png" Stretch="None" HorizontalAlignment="Center" VerticalAlignment="Center"/>

                <!--begin preview-->
                <StackPanel x:Name="PreviewHolder" DataContext="{Binding ElementName=Options, Path=SelectedItem}" VerticalAlignment="Center" Margin="0,82,30,0" Width="1350" Height="1600" Background="Transparent">

                    <Control Template="{StaticResource OptionPreview}" HorizontalAlignment="Center" Margin="0,80,0,0"/>

                    <TextBlock Text="{Binding Text}"
                                HorizontalAlignment="Center" Margin="0,28,0,60"
                                FontSize="{DynamicResource BigFontSize}" Foreground="{DynamicResource LS_tint100}"
                                TextAlignment="Center"/>

                    <TextBlock Text="{Binding Description}"
                                HorizontalAlignment="Center" Width="1280"
                                FontSize="{DynamicResource DefaultFontSize}" Foreground="{DynamicResource LS_specialTxtColor}"
                                TextAlignment="Left" TextWrapping="Wrap"/>

                    <StackPanel.Style>
                        <Style TargetType="{x:Type StackPanel}">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding HasPreview}" Value="False">
                                    <Setter Property="Visibility" Value="Hidden"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding ListItemType}" Value="None">
                                    <Setter Property="Visibility" Value="Hidden"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding ListItemType}" Value="Button">
                                    <Setter Property="Visibility" Value="Hidden"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding ListItemType}" Value="Keybinding">
                                    <Setter Property="Visibility" Value="Hidden"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding ListItemType}" Value="String">
                                    <Setter Property="Visibility" Value="Hidden"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </StackPanel.Style>
                </StackPanel>
            </StackPanel>

            <Control Template="{StaticResource SettingsChangeNotificationTemplate}" VerticalAlignment="Bottom" Margin="0,0,0,140"/>

            <Control Template="{StaticResource OptionBottomButtons}" VerticalAlignment="Bottom" HorizontalAlignment="Center"/>
        </Grid>
    </ControlTemplate>

    <ControlTemplate x:Key="OptionBGAndHeaderTemplate">
        <Grid Background="{StaticResource LS_PanelBackground}">
            <Image Source="pack://application:,,,/GustavNoesisGUI;component/Assets/Shared/mainpane_vignette.png" Stretch="Fill"/>
            <Image Source="pack://application:,,,/GustavNoesisGUI;component/Assets/Options/options_midColour_bg.png" Stretch="UniformToFill" HorizontalAlignment="Center"/>
        </Grid>
    </ControlTemplate>

    <ControlTemplate x:Key="KeybindingOptionsTemplate">
        <Grid HorizontalAlignment="Center"  Height="{StaticResource DesignedWindowHeight}" Width="{StaticResource DesignedWindowWidth}">

            <Control DataContext="{Binding DataContext, RelativeSource={RelativeSource AncestorType={x:Type ls:UIWidget}}}" Template="{StaticResource OptionTopButtons}"/>

            <Grid VerticalAlignment="Center" HorizontalAlignment="Center" Margin="0,0,0,40" >

                <ls:LSListBox AlternationCount="{Binding Options.Count}" 
                              SelectedIndex="1"
                              ItemsSource="{Binding Options}" 
                              ItemContainerStyle="{StaticResource OptionStyle}" 
                              Background="Transparent" BorderBrush="Transparent" 
                              Height="1600" Width="2800"
                              HorizontalAlignment="Center" VerticalAlignment="Center"
                              ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                              LocalFocusSelector="{Binding ElementName=Selector,Mode=OneWay}"
                              ActionPrevEvent="UIUp" ActionNextEvent="UIDown">
                    <ListBox.Template>
                        <ControlTemplate>
                            <ScrollViewer Template="{StaticResource ScrollViewerTemplate}" Padding="150,0,0,0" Focusable="False">
                                <ScrollViewer.Resources>
                                    <GridLength x:Key="Top">84</GridLength>
                                    <GridLength x:Key="Bottom">84</GridLength>
                                </ScrollViewer.Resources>
                                <ItemsPresenter/>
                            </ScrollViewer>
                        </ControlTemplate>
                    </ListBox.Template>
                </ls:LSListBox>

                <Control x:Name="Selector" IsHitTestVisible="False" VerticalAlignment="Center" HorizontalAlignment="Center" Template="{StaticResource SelectorTemplate}" Visibility="Hidden"/>
            </Grid>

            <Control Template="{StaticResource OptionBottomButtons}" VerticalAlignment="Bottom"/>

            <Grid HorizontalAlignment="Center" VerticalAlignment="Center" Width="3840" Height="2160" Background="{StaticResource LS_PanelBackground}"
                  Visibility="{Binding InputBindingVisible, Converter={StaticResource BoolToVisibleConverter}}">
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Orientation="Vertical">
                    <TextBlock Text="{Binding ActiveBindingKey}" TextAlignment="Center"
                               FontSize="{DynamicResource HugeFontSize}" Foreground="{StaticResource LS_tint100}" Background="Transparent" FontWeight="Bold" Margin="0,20,0,20"/>
                    <TextBlock Text="{Binding ActiveBindingName}" TextAlignment="Center"
                               FontSize="{DynamicResource HugeFontSize}" Foreground="{StaticResource LS_tint100}" Background="Transparent" FontWeight="Bold" Margin="0,20,0,20"/>
                    <StackPanel Orientation="Horizontal" Margin="10,0,10,0">
                        <ls:LSButton 
                                Style="{StaticResource BrownButtonStyle}" 
                                Content="{Binding Source='hd58c82e5ga514g4c6ag9f1eg29134bf7e78b', Converter={StaticResource TranslatedStringConverter}}" 
                                Command="{Binding ClearSingleBinding}" />
                        <ls:LSButton 
                                Style="{StaticResource BrownButtonStyle}" 
                                Content="{Binding Source='h675a55aegd07cg4ab0g862dg0c564197d478', Converter={StaticResource TranslatedStringConverter}}" 
                                Command="{Binding CancelSingleBinding}"/>
                        <ls:LSButton 
                                Style="{DynamicResource BlueButtonStyle}" 
                                Content="{Binding Source='hd9ef0e80g254cg4b35gb1e7gadfd05fe9f25', Converter={StaticResource TranslatedStringConverter}}" 
                                Command="{Binding ApplySingleBinding}"/>
                    </StackPanel>
                </StackPanel>
            </Grid>
        </Grid>
    </ControlTemplate>

</ResourceDictionary>