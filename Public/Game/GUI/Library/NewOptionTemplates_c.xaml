<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
			        xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
					xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                    xmlns:System="clr-namespace:System;assembly=mscorlib"
                    xmlns:ls="clr-namespace:ls;assembly=SharedGUI"
                    xmlns:noesis="clr-namespace:NoesisGUIExtensions;assembly=Noesis.GUI.Extensions"
					mc:Ignorable="d">


    <BitmapImage x:Key="ListButtonBg" UriSource="pack://application:,,,/GustavNoesisGUI;component/Assets/Shared_c/c_list_buttonBG_generalAlt.png"/>
    <BitmapImage x:Key="ListButtonBg_h" UriSource="pack://application:,,,/GustavNoesisGUI;component/Assets/Shared_c/c_list_buttonBG_h.png"/>

    <BitmapImage x:Key="ListItemFocused1" UriSource="pack://application:,,,/GustavNoesisGUI;component/Assets/Shared_c/c_list_buttonBG_h1.png"/>
    <BitmapImage x:Key="ListItemFocused2" UriSource="pack://application:,,,/GustavNoesisGUI;component/Assets/Shared_c/c_list_buttonBG_h2.png"/>
    <BitmapImage x:Key="ListItemFocused3" UriSource="pack://application:,,,/GustavNoesisGUI;component/Assets/Shared_c/c_list_buttonBG_h2.png"/>

    <BitmapImage x:Key="OptionArrow_d" UriSource="pack://application:,,,/GustavNoesisGUI;component/Assets/Shared_c/c_carousel_arrow_d.png"/>
    <BitmapImage x:Key="OptionArrowDisabled" UriSource="pack://application:,,,/GustavNoesisGUI;component/Assets/Shared_c/c_carousel_arrow_disabled.png"/>

    <Style x:Key="FocusedListItemTitleText" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="{StaticResource DefaultFont}"/>
        <Setter Property="FontSize" Value="{StaticResource ScaledDefaultFontSize}"/>
        <Setter Property="Foreground" Value="{StaticResource LS_accent75TxtColor}"/>
        <Setter Property="HorizontalAlignment" Value="Center"/>
    </Style>
    <Style x:Key="FocusedListItemValueText" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="{StaticResource DefaultFont}"/>
        <Setter Property="FontSize" Value="{StaticResource ScaledDefaultFontSize}"/>
        <Setter Property="Foreground" Value="{StaticResource LS_accent100TxtColor}"/>
        <Setter Property="HorizontalAlignment" Value="Center"/>
    </Style>

    <ControlTemplate x:Key="IconOptionArrow">
        <Image x:Name="arrow" Source="{StaticResource OptionArrow_d}" Stretch="None"/>
        <ControlTemplate.Triggers>
            <Trigger Property="IsEnabled" Value="False">
                <Setter TargetName="arrow" Property="Source" Value="{StaticResource OptionArrowDisabled}"/>
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <DataTemplate DataType="{x:Type ls:VMTickBoxSetting}">
        <Grid VerticalAlignment="Center">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <ls:LSButton x:Name="leftBtn" Grid.Row="0" Grid.RowSpan="2" BoundEvent="UILeft" Template="{StaticResource IconOptionArrow}" HorizontalAlignment="Left" VerticalAlignment="Center"
                         Margin="40,0,0,0" Command="{Binding DataContext.SwapTickItemCommand, RelativeSource={RelativeSource AncestorType={x:Type ls:UIWidget}}}"
                         CommandParameter="{Binding .}">
                <ls:LSButton.RenderTransform>
                    <ScaleTransform ScaleX="-1" CenterX="32"/>
                </ls:LSButton.RenderTransform>
            </ls:LSButton>
            <ls:LSButton x:Name="rightBtn" Grid.Row="0" Grid.RowSpan="2" BoundEvent="UIRight" Template="{StaticResource IconOptionArrow}" HorizontalAlignment="Right" VerticalAlignment="Center"
                         Margin="0,0,40,0" Command="{Binding DataContext.SwapTickItemCommand, RelativeSource={RelativeSource AncestorType={x:Type ls:UIWidget}}}"
                         CommandParameter="{Binding .}"/>

            <TextBlock Grid.Row="0" Style="{StaticResource FocusedListItemTitleText}" Text="{Binding Text}"/>
            <TextBlock Grid.Row="1" Style="{StaticResource FocusedListItemValueText}" Text="{Binding Source='h14129139ge40bg4627g944ag06fec7d74c9d', Converter={StaticResource TranslatedStringConverter}}"
                       Visibility="{Binding Value, Converter={StaticResource BoolToVisibleConverter}}"/>
            <TextBlock Grid.Row="1" Style="{StaticResource FocusedListItemValueText}" Text="{Binding Source='h366d1022g0f83g4132ga3a6g733b9df2c8e7', Converter={StaticResource TranslatedStringConverter}}"
                       Visibility="{Binding Value, Converter={StaticResource BoolToCollapsedConverter}}"/>
        </Grid>
    </DataTemplate>

    <DataTemplate DataType="{x:Type ls:VMComboBoxSetting}">
        <Grid VerticalAlignment="Center">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <ls:LSButton x:Name="leftBtn" Grid.Row="0" Grid.RowSpan="2" BoundEvent="UILeft" Template="{StaticResource IconOptionArrow}" HorizontalAlignment="Left" VerticalAlignment="Center"
                         Margin="40,0,0,0" Command="{Binding DataContext.PrevComboItemCommand, RelativeSource={RelativeSource AncestorType={x:Type ls:UIWidget}}}"
                         CommandParameter="{Binding .}">
                <ls:LSButton.RenderTransform>
                    <ScaleTransform ScaleX="-1" CenterX="32"/>
                </ls:LSButton.RenderTransform>
            </ls:LSButton>
            <ls:LSButton x:Name="rightBtn" Grid.Row="0" Grid.RowSpan="2" BoundEvent="UIRight" Template="{StaticResource IconOptionArrow}" HorizontalAlignment="Right" VerticalAlignment="Center"
                         Margin="0,0,40,0" Command="{Binding DataContext.NextComboItemCommand, RelativeSource={RelativeSource AncestorType={x:Type ls:UIWidget}}}"
                         CommandParameter="{Binding .}"/>

            <TextBlock Grid.Row="0" Style="{StaticResource FocusedListItemTitleText}" Text="{Binding Text}"/>
            <TextBlock Grid.Row="1" Style="{StaticResource FocusedListItemValueText}" Text="{Binding SelectedItem.Name}"/>
        </Grid>
    </DataTemplate>

    <Style x:Key="OptionStyle" TargetType="ContentPresenter">
        <Setter Property="ContentTemplate">
            <Setter.Value>
                <DataTemplate DataType="{x:Type ls:VMListItem}">
                    <Grid Width="890" Height="180">
                        <Image x:Name="Box" Source="{StaticResource ListButtonBg}" Stretch="Fill"/>

                        <ContentPresenter x:Name="Content" IsEnabled="False" Content="{Binding .}"/>

                        <Control x:Name="Frame" Visibility="Hidden" Style="{StaticResource FocusableBorderStyle}" Width="930" Height="216"
                                 HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Grid>
                    <DataTemplate.Triggers>
                        <Trigger Property="ls:MoveFocus.IsFocused" Value="True">
                            <Setter TargetName="Box" Property="Source" Value="{StaticResource ListButtonBg_h}"/>
                            <Setter TargetName="Content" Property="IsEnabled" Value="True"/>
                            <Setter TargetName="Frame" Property="Visibility" Value="Visible"/>
                        </Trigger>
                    </DataTemplate.Triggers>
                </DataTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="HorizontalAlignment" Value="Center"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="Margin" Value="0,5"/>
        <Setter Property="Focusable" Value="True"/>
        <Setter Property="ls:MoveFocus.Focusable" Value="True"/>
    </Style>

    <ControlTemplate x:Key="ManualOptionTemplate" TargetType="ContentControl">
        <Grid Width="890" Height="180">
            <Image x:Name="Box" Source="{StaticResource ListButtonBg}" Stretch="Fill"/>

            <ContentPresenter x:Name="Content" IsEnabled="False" Content="{TemplateBinding Content}" ContentTemplate="{TemplateBinding ContentTemplate}"/>

            <Control x:Name="Frame" Visibility="Hidden" Style="{StaticResource FocusableBorderStyle}" Width="930" Height="216"
                     HorizontalAlignment="Center" VerticalAlignment="Center" />
        </Grid>
        <ControlTemplate.Triggers>
            <Trigger Property="ls:MoveFocus.IsFocused" Value="True">
                <Setter TargetName="Box" Property="Source" Value="{StaticResource ListButtonBg_h}"/>
                <Setter TargetName="Content" Property="IsEnabled" Value="True"/>
                <Setter TargetName="Frame" Property="Visibility" Value="Visible"/>
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <Style x:Key="ManualOptionStyle" TargetType="ContentControl">
        <Setter Property="Template" Value="{StaticResource ManualOptionTemplate}" />
        <Setter Property="HorizontalAlignment" Value="Center"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="Margin" Value="0,5"/>
        <Setter Property="Focusable" Value="True"/>
        <Setter Property="ls:MoveFocus.Focusable" Value="True"/>
    </Style>

</ResourceDictionary>